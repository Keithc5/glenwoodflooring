---
import Section from '@components/ui/Section.astro';
import SectionHeading from '@components/ui/SectionHeading.astro';
import Grid from '@components/ui/Grid.astro';
import { withBasePath } from '@utils/site-url';
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface GalleryItem {
  title: string;
  image: ImageMetadata;
  alt: string;
  href?: string;
}

interface Props {
  eyebrow?: string;
  title: string;
  description?: string;
  items: GalleryItem[];
  columns?: 2 | 3 | 4;
}

const {
  eyebrow,
  title,
  description,
  items,
  columns = 3,
} = Astro.props;
---

<Section>
  <div class="gallery">
    <SectionHeading
      eyebrow={eyebrow}
      title={title}
      description={description}
      align="center"
    />

    <Grid columns={columns} gap="md">
      {items.map((item) => (
        item.href ? (
          <a href={withBasePath(item.href)} class="gallery-item">
            <Image
              class="gallery-item__image"
              src={item.image}
              alt={item.alt}
              width="800"
              height="600"
              loading="lazy"
            />
            <span class="gallery-item__title">{item.title}</span>
          </a>
        ) : (
          <figure class="gallery-item">
            <Image
              class="gallery-item__image"
              src={item.image}
              alt={item.alt}
              width="800"
              height="600"
              loading="lazy"
            />
            <figcaption class="gallery-item__title">{item.title}</figcaption>
          </figure>
        )
      ))}
    </Grid>
  </div>
</Section>
